# Backend API host URL
NEXT_PUBLIC_API_HOST=

# ⚠️ SECURITY WARNING: NEXT_PUBLIC_* variables are exposed to the browser!
# Do not use this for storing actual secrets or authentication tokens.
# The current codebase uses this for legacy API access, but user authentication
# is handled via session-token stored in localStorage after login.
NEXT_PUBLIC_API_TOKEN=